"""Placeholder for the planned Flask web UI.

The web layer will import ``rossum_user_loader.core`` (and reuse the same
user-loading logic the CLI uses) and serve a browser front end that replaces
the spreadsheet as the input/output interface. See docs/ for the page plan.

Nothing is wired up yet; this package exists so the structure is ready.
"""
